let ranningTotal = 0;
let buffer = "0";
let previousOperator = 0 ;

const screen = document.querySelector('.screen');

function buttonClick(value){
    if(isNaN(value)){
        handleSymbol(value);
        else{
            
        }
    }
}
